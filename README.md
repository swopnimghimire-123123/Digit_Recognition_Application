# Digit_Recognition_Application
An interactive web application that allows users to draw handwritten digits and classify them in real time using a Convolutional Neural Network (CNN) trained on the MNIST dataset.
